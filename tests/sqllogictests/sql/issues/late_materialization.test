# name: test/sql/issues/late_materialization.test
# description: Test late materialization
# group: [issues]
# adapted: removed USE ducklake, use fully-qualified table names

require ducklake

require parquet

test-env DUCKLAKE_CONNECTION __TEST_DIR__/{UUID}.db

test-env DATA_PATH __TEST_DIR__


statement ok
ATTACH 'ducklake:${DUCKLAKE_CONNECTION}' AS ducklake (DATA_PATH '${DATA_PATH}/ducklake_late_materialization');

statement ok
CREATE TABLE ducklake.my_table(id INTEGER, value VARCHAR);

statement ok
INSERT INTO ducklake.my_table VALUES (1, 'hello');

statement ok
INSERT INTO ducklake.my_table VALUES (2, 'world');

statement ok
INSERT INTO ducklake.my_table VALUES (3, 'this');

statement ok
INSERT INTO ducklake.my_table VALUES (4, 'is');

statement ok
INSERT INTO ducklake.my_table VALUES (5, 'a');

statement ok
INSERT INTO ducklake.my_table VALUES (6, 'test');

query IT
SELECT * FROM ducklake.my_table WHERE id > 3 ORDER BY value DESC LIMIT 1
----
6	test
